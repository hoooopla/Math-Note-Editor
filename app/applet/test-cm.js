const { RangeSet } = require("@codemirror/state");
// try to see if sort flag sorts or assumes sorted
try {
    let decos = [];
    // We can't actually do this without imports properly compiled
} catch(e) {}

